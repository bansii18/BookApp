<!DOCTYPE html>
<html>
<head>
  <title>Edit Book</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-white p-4">
  <h1 class="text-center mb-4">Edit Book</h1>
  <div class="container">
    <form method="POST" action="/books/<%= book._id %>">
      <div class="mb-3">
        <label>Title</label>
        <input class="form-control" name="title" value="<%= book.title %>" required>
      </div>
      <div class="mb-3">
        <label>Description</label>
        <textarea class="form-control" name="description" required><%= book.description %></textarea>
      </div>
      <div class="mb-3">
        <label>Author</label>
  
         <input class="form-control" value="<%= book.author %>" disabled>
      </div>
      <div class="mb-3">
        <label>Category</label>
        <input class="form-control" name="category" value="<%= book.category %>" required>
      </div>
      <div class="mb-3">
        <label>Rating (0–5)</label>
        <input type="number" class="form-control" name="rating" value="<%= book.rating %>" min="0" max="5" required>
      </div>
      <div class="mb-3">
        <label>Image URL</label>
        <input class="form-control" name="image" value="<%= book.image %>">
      </div>
      <button class="btn btn-primary">Update Book</button>
    </form>
  </div>
</body>
</html>